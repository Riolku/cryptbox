import styles from '../styles/FolderPath.module.css';

const FolderPath = ({ folderPath }: { folderPath: any }) => {
    return (
        <div className = { styles.pathContainer }>
            
        </div>
    );
}

export default FolderPath;